@"
# Практична робота 5

## Завдання 4

Для перевірки DEFAULT було додано новий предмет без зазначення статусу:

Комп'ютерні мережі — 60 годин.

Після виконання SELECT було підтверджено, що для стовпця status автоматично встановилося значення `активний`.

## Завдання 5

При виконанні UPDATE:

```sql
UPDATE subjects
SET hours = -5
WHERE name = 'Операційні системи';